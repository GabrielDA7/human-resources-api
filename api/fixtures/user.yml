App\Entity\User:
    user_1:
        email: "user@gmail.com"
        roles: ["ROLE_USER"]
        password: <encode(@self, "pwd")>
        confirmationToken: <regexify('[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,4}')>
        enabled: <boolean()>
